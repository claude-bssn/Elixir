defmodule MyAppWeb.PageView do
  use MyAppWeb, :view


  # def render("index.json", %{pages: pages}) do
  #   %{data: render_many(pages, MyAppWeb.PageView, "page.json")}
  # end

  # def render("show.json", %{page: page}) do
  #   %{data: render_one(page, MyAppWeb.PageView, "page.json")}
  # end

  # def render("page.json", %{page: page}) do
  #   %{title: page.title}
  # end
  # def render("index.html", )
  # def list() do

  #   Enum.map(@champions, fn(row)-> Enum.to_list(row["tags"]) end)




  # end
end
